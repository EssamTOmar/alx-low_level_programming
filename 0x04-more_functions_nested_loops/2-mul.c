#include "main.h"
#include <stdio.h>

/**
 * mul - multiblies two numbers
 * @a: first number to be multiblied
 * @b: second number to be multiblied
 * Return: the multiblication final result
 */
int mul(int a, int b)
{
	printf("%d\n", mul(a, b));
}
