#include <stdio.h>
/**
 * main - Entry Point
 * a program to print the first ten numbers from 0 to 9
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d\n", a);
	return (0);
}
